
  # Menu Match UI (Copy)

  This is a code bundle for Menu Match UI (Copy). The original project is available at https://www.figma.com/design/UsqengbVHp7ofIUk7iuyWa/Menu-Match-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  